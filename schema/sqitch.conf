[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "todo"]
	uri = db:pg:todo_app
[engine "pg"]
	target = todo
[deploy]
	verify = true
