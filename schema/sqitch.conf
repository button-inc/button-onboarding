[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "todo_app"]
	uri = db:pg:todo_app
[engine "pg"]
	target = todo_app
[deploy]
	verify = true
